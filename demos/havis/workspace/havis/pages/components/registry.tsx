// Workspace component registry — aliased at build time as `@wsx/registry`.
// No custom widgets yet; the image thumbnails come from core's `format: image`.
export const components: Record<string, React.ComponentType<Record<string, unknown>>> = {};
