slug: havis-web
name: havis.com (public site)
description: |
  Havis's public product site — verticals, product lines, docking and
  console families — so the coworkers can speak Havis's language. Manual
  sync only; nothing crawls on its own. Does not read robots.txt: confirm
  crawl permission before raising maxPages.
kind: web
config:
  crawl:
    startUrl: https://www.havis.com/
    maxDepth: 1
    maxPages: 25
enabled: true
